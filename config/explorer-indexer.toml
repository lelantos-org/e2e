# Explorer indexer config. Mounted at /etc/explorer-indexer.toml in the container.

database_url = "postgres://postgres:postgres@postgres:5432/postgres"
erc20_metadata_timeout_ms = 2000
rpc_urls = []

# Optional per-chain RPC for ERC20 metadata enrichment. The event pipeline does
# not need RPC — asset metadata comes from AssetRegistered — so this can stay
# empty.
# [[rpc_urls]]
# chain_id = 1
# url = "http://host.docker.internal:8545"
